// Sempre quando a instrucao pra o compuatdor for uma ação

function soma(numero1, numero2) {
  const resultado = numero1 + numero2
  console.log(resultado)
}

// nao posso esquecer de chamar essa funcao passando os parametros
soma(20,30)

function subtrai(num1, num2) {
  const resultado = num1 - num2
  console.log(resultado)
}

subtrai(10, 3)